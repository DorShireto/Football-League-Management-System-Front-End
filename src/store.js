const state = {

    server_domain: "http://localhost:",
    server_port: "3000"
};

const actions = {
    // addItem: () => {
    //     state.items.push("newItem")
    // }
}

export { state, actions }